using Ojaswat.Models;

namespace Ojaswat.Renderers;

/// <summary>
/// Debit Note — uses all DefaultRenderer / RendererBase defaults.
/// Title driven by DocLabel("DebitNote") → "Debit Note".
/// </summary>
public sealed class DebitNoteRenderer : DefaultRenderer
{
    // All defaults apply — no overrides required.
}
