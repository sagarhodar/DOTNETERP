using Ojaswat.Models;

namespace Ojaswat.Renderers;

/// <summary>
/// Credit Note — uses all DefaultRenderer / RendererBase defaults.
/// Title driven by DocLabel("CreditNote") → "Credit Note".
/// </summary>
public sealed class CreditNoteRenderer : DefaultRenderer
{
    // All defaults apply — no overrides required.
}
